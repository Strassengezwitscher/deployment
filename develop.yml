---

- name: Deploy django develop branch to server
  hosts: strassengezwitscher
  remote_user: deploy
  become_user: root
  become: yes
  vars_files:
    - group_vars/sensitive.yml

  roles:
    - { role: strassengezwitscher, dir: 'strassengezwitscher_dev', branch: 'develop' }
